import processing.core.PImage;

import java.util.List;

public class GoldOre extends Ore {
    public GoldOre(String id, Point position, int actionPeriod,
               List<PImage> images) {
        super(id, position, actionPeriod, images);
    }
}
